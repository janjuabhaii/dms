import DashboardLayout from "./DashboardLayout";
import { adminNavItems } from "@/lib/navConfig";

const AdminLayout = () => <DashboardLayout navItems={adminNavItems} />;

export default AdminLayout;
