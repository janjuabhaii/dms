# Base URL is not needed for API calls in dev (Vite proxies /api -> backend),
# but is useful if you ever split frontend/backend across different hosts.
VITE_API_BASE_URL=/api/v1
VITE_SOCKET_URL=http://localhost:5000


# backend
https://dms-pi-black.vercel.app/